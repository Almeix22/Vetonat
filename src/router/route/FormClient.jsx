import React from 'react';
import CreationClient from '../../components/Formulaire/CreationClient';
import Header from '../../components/global/Header.jsx';

function FormClient() {
    return (
        <>
            <Header />
            <CreationClient />
        </>
    );
}

export default FormClient;
